import type { CSSProperties } from "react";

// Small shared styles used across the demo panels.
export const card: CSSProperties = {
  border: "1px solid #e5e7eb",
  borderRadius: 12,
  padding: "1rem 1.25rem",
  marginBottom: "1rem",
};

export const errStyle: CSSProperties = { color: "#b91c1c", fontSize: 13 };

export const mono: CSSProperties = { fontSize: 12, wordBreak: "break-all" };
